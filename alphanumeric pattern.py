# Input a string
input_string = input("Enter a string: ")

# Check if the string is alphanumeric
if input_string.isalnum():
    print("The input string is alphanumeric.")
else:
    print("The input string is not alphanumeric.")
